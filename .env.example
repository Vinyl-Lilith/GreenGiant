# MongoDB Connection
MONGODB_URI=mongodb+srv://BioCube:nyT3l8zwqXg9P2nV@cluster0.rzrhbx6.mongodb.net/?appName=Cluster0

# JWT Secret (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=woahasuperlongandkindamonotonouskey?

# JWT Expiry
JWT_EXPIRE=7d

# API Key for Raspberry Pi communication (must match Pi's API_KEY)
PI_API_KEY=idkthisshouldpropablybegoodeneoughiguess2

# Server Port (Render will override this)
PORT=10000

# Node Environment
NODE_ENV=production

# CORS Origins (comma-separated, no spaces)
CORS_ORIGINS=https://idk-six-olive.vercel.app,https://idk-ksas6cw1u-vinyl-liliths-projects.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Session Settings
SESSION_TIMEOUT_MINUTES=60
